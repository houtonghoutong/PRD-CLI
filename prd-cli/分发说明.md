# 📦 PRD CLI 工具 - 分发说明

## ✅ 打包完成

安装包已成功生成，可以开始内部测试了！

---

## 📄 分发文件清单

请将以下文件打包发送给测试人员：

```
📦 PRD-CLI-测试包/
  ├── prd-workflow-cli-1.0.0.tgz     ← npm 安装包 (必需)
  ├── install.sh                      ← 自动安装脚本 (推荐)
  ├── 内部测试安装指南.md             ← 安装和测试说明 (必需)
  ├── README.md                       ← 工具使用文档 (推荐)
  └── 权责划分修复报告.md             ← 新功能说明 (可选)
```

---

## 🚀 快速分发方式

### 方式 1: 创建测试包压缩文件

在当前目录执行：
```bash
cd "/Users/hou/Documents/UGit/PRD CLI/prd-cli"

# 创建测试包目录
mkdir -p ~/Desktop/PRD-CLI-测试包

# 复制必需文件
cp prd-workflow-cli-1.0.0.tgz ~/Desktop/PRD-CLI-测试包/
cp install.sh ~/Desktop/PRD-CLI-测试包/
cp 内部测试安装指南.md ~/Desktop/PRD-CLI-测试包/
cp README.md ~/Desktop/PRD-CLI-测试包/
cp 权责划分修复报告.md ~/Desktop/PRD-CLI-测试包/

# 打包
cd ~/Desktop
zip -r PRD-CLI-测试包.zip PRD-CLI-测试包/

echo "✅ 测试包已创建: ~/Desktop/PRD-CLI-测试包.zip"
```

### 方式 2: 通过 Git 仓库分发

如果有内部 Git 仓库：
```bash
cd "/Users/hou/Documents/UGit/PRD CLI/prd-cli"

# 初始化 Git（如果还没有）
git init
git add .
git commit -m "v1.0.0: 初始版本，集成PM确认机制和对话归档"
git tag v1.0.0

# 推送到远程仓库
git remote add origin <你的仓库地址>
git push -u origin main
git push --tags
```

测试人员可以：
```bash
git clone <仓库地址>
cd prd-cli
./install.sh
```

### 方式 3: 网络共享

上传到内部文件服务器或云盘：
- 钉钉群文件
- 企业网盘
- 内部文件服务器

---

## 📝 给测试人员的说明

发送测试包时，附上这段说明：

```
Hi 团队，

现在可以开始测试新的 PRD CLI 工具了！

【快速安装】
解压后，在终端执行：
  cd PRD-CLI-测试包
  ./install.sh

【新功能亮点】
✅ PM 确认机制 - 关键决策点需要 PM 确认，防止 AI 越权
✅ 对话归档 - 所有决策自动归档，完全可追溯
✅ 权责明确 - 清晰显示 PM/AI 职责边界

【测试重点】
测试 B1 创建时的 PM 确认流程：
  prd init 测试项目
  cd 测试项目
  prd iteration new
  prd plan create B1  ← 注意这里会要求PM确认！

详细说明请查看《内部测试安装指南.md》

有问题随时在群里反馈！
```

---

## ✅ 安装验证检查项

测试人员安装后，请确认：

- [ ] `prd --version` 显示 `1.0.0`
- [ ] `prd --help` 正常显示帮助信息
- [ ] 可以创建测试项目
- [ ] B1 创建时弹出 PM 确认
- [ ] 对话归档目录自动创建
- [ ] 权责提示正常显示

---

## 🐛 收集反馈

建议创建一个反馈表单或文档，收集：

1. **安装问题**
   - 操作系统和版本
   - Node.js 版本
   - 遇到的错误信息

2. **功能问题**
   - 哪个命令有问题
   - 期望行为 vs 实际行为
   - 复现步骤

3. **体验建议**
   - 提示信息是否清晰
   - 流程是否合理
   - 改进建议

---

## 🔄 版本更新流程

如果需要发布新版本：

1. 修改代码
2. 更新 `package.json` 中的 `version`
3. 重新打包：`npm pack`
4. 更新安装脚本中的版本号
5. 重新分发

---

## 📊 当前版本信息

- **版本号**: 1.0.0
- **包名**: prd-workflow-cli
- **包大小**: 25.2 KB
- **包含文件**: 15 个
- **Node.js 要求**: >= 14.0.0

---

## 📞 技术支持

**安装包位置**: 
`/Users/hou/Documents/UGit/PRD CLI/prd-cli/prd-workflow-cli-1.0.0.tgz`

**源码位置**: 
`/Users/hou/Documents/UGit/PRD CLI/prd-cli/`

**关键文档**:
- `README.md` - 使用说明
- `GUIDE.md` - 详细指南  
- `权责划分修复报告.md` - 权责机制详解
- `内部测试安装指南.md` - 测试说明

---

**准备就绪，可以开始分发测试了！** 🎉

---
生成时间: ${new Date().toLocaleString('zh-CN')}
